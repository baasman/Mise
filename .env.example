# Postgres connection (local dev default; adjust user/host as needed)
DATABASE_URL="postgresql://localhost:5432/mise?schema=public"

# Anthropic API key — server-side only, never exposed to the client
ANTHROPIC_API_KEY=""
